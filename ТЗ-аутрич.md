# Техническое задание: сервис персонального B2B-аутрича

## 1. Назначение продукта

Сервис нужен для персонального B2B-аутрича: подготовки индивидуальных писем, отправки цепочек follow-up, остановки автоматической цепочки после ответа, хранения полной истории переписки и последующего анализа диалогов с помощью ИИ.

Это не классический сервис массовой рассылки. Основная ценность продукта — помогать пользователю вести управляемые персональные касания:

1. Подготовить индивидуальные письма в Excel.
2. Загрузить Excel в сервис.
3. Проверить письма, получателей, почтовые ящики и лимиты.
4. Отправить первое письмо и follow-up цепочку.
5. Остановить автоматическую цепочку, если человек ответил.
6. Принять ручное решение по ответу.
7. Хранить всю переписку в диалогах.
8. Выгружать диалоги для AI-анализа.

## 2. Цель MVP

Собрать рабочий инструмент, который позволяет пользователю:

- загрузить Excel с персональными письмами;
- превратить строки Excel в черновики писем;
- проверить ошибки перед отправкой;
- поставить готовые письма в очередь;
- отправлять письма через подключенные SMTP mailbox;
- читать входящие через IMAP;
- собирать исходящие и входящие письма в единые диалоги;
- автоматически останавливать follow-up цепочку после ответа;
- показывать список диалогов, требующих решения;
- экспортировать диалоги для анализа ИИ.

## 3. Ключевой пользовательский сценарий

### 3.1. Подготовка

Пользователь вручную собирает базу и пишет персональные письма в Excel.

В Excel каждая строка может содержать:

- получателя;
- компанию;
- тему письма;
- тело письма;
- сегмент;
- заметки;
- mailbox отправки;
- follow-up шаги.

### 3.2. Импорт

Пользователь загружает Excel в сервис.

Сервис:

- читает строки;
- проверяет обязательные поля;
- ищет дубли;
- проверяет email;
- сопоставляет mailbox;
- создает черновики;
- показывает отчет по ошибкам.

Импорт не должен сразу отправлять письма.

### 3.3. Проверка черновиков

Пользователь видит список черновиков:

- готовые к отправке;
- с ошибками;
- пропущенные;
- уже существующие дубли.

Пользователь может открыть черновик и исправить:

- тему;
- текст;
- email;
- компанию;
- mailbox;
- дату отправки;
- follow-up шаги.

### 3.4. Отправка

Пользователь ставит готовые черновики в очередь.

Сервис отправляет письма с учетом:

- SMTP/IMAP готовности mailbox;
- лимитов mailbox;
- рабочего окна;
- задержек между письмами;
- suppression list;
- preflight-проверки;
- dry-run режима;
- open tracking, если включен.

### 3.5. Follow-up цепочка

Если получатель не ответил, сервис может отправлять follow-up шаги по расписанию.

Пример:

1. День 0 — первое письмо.
2. День 3 — follow-up 1.
3. День 7 — follow-up 2.
4. День 14 — финальное письмо.

### 3.6. Ответ получателя

Если пришел ответ от получателя:

- сервис сохраняет входящее письмо;
- связывает его с диалогом;
- классифицирует ответ;
- останавливает автоматическую цепочку;
- отменяет будущие pending/retrying follow-up письма;
- помещает диалог в раздел "Требуют решения".

После ответа сервис не должен продолжать цепочку автоматически без разрешения пользователя.

## 4. Основные разделы интерфейса

### 4.1. Импорт Excel

Функции:

- загрузить `.xlsx`;
- загрузить `.csv`;
- показать предпросмотр строк;
- сопоставить колонки;
- показать ошибки;
- создать черновики;
- скачать отчет по ошибкам.

### 4.2. Черновики

Функции:

- список персональных писем;
- фильтр по статусу;
- фильтр по кампании/импорту;
- фильтр по ошибкам;
- предпросмотр письма;
- редактирование темы и текста;
- выбор mailbox;
- массовая постановка готовых черновиков в очередь;
- отмена черновика.

### 4.3. Отправка

Функции:

- очередь писем;
- расписание отправки;
- статус каждого письма;
- время следующей отправки;
- текущий mailbox;
- ошибки SMTP;
- отмена письма;
- повторная отправка;
- ручное подтверждение, если включено.

### 4.4. Диалоги

Функции:

- список всех диалогов;
- фильтр по статусу;
- фильтр по кампании;
- фильтр по сегменту;
- фильтр по классификации ответа;
- карточка диалога;
- вся переписка по email;
- исходящие письма;
- входящие ответы;
- открытия;
- заметки;
- решения пользователя.

### 4.5. Требуют решения

Отдельный рабочий экран для входящих ответов, после которых цепочка остановлена.

Для каждого диалога пользователь видит:

- кто ответил;
- на какое письмо ответил;
- полный текст ответа;
- классификацию;
- историю переписки;
- рекомендуемое действие.

Доступные действия:

- остановить цепочку;
- продолжить цепочку;
- ответить вручную;
- сгенерировать ответ с ИИ;
- отложить follow-up;
- отметить как positive;
- отметить как negative;
- отметить как not target;
- отметить как auto-reply;
- добавить email или домен в stop-list.

### 4.6. Аналитика

Метрики должны считаться по outreach, без прогрева и тестовых писем.

Основные метрики:

- импортировано строк;
- создано черновиков;
- готово к отправке;
- заблокировано из-за ошибок;
- отправлено первых писем;
- отправлено follow-up;
- уникальные открытия;
- уникальные диалоги с ответом;
- положительные ответы;
- негативные ответы;
- автоответы;
- недоставки;
- цепочки остановлены из-за ответа;
- требуют решения;
- среднее время до ответа;
- количество касаний до ответа;
- reply rate;
- positive reply rate.

### 4.7. Экспорт для ИИ

Функции:

- экспорт диалогов в `jsonl`;
- экспорт диалогов в `json`;
- экспорт в `csv`;
- экспорт в `xlsx`;
- фильтр перед экспортом.

Фильтры:

- кампания;
- импорт;
- сегмент;
- mailbox;
- статус диалога;
- классификация ответа;
- дата отправки;
- дата ответа;
- только с ответами;
- только positive;
- только negative;
- только без ответа.

## 5. Excel-импорт

### 5.1. Минимальные колонки

Обязательные:

- `email`;
- `subject`;
- `body`.

Желательные:

- `company`;
- `contact`;
- `position`;
- `website`;
- `segment`;
- `city`;
- `mailbox`;
- `send_after`;
- `notes`;
- `source`.

### 5.2. Follow-up колонки

Поддержать wide-format:

- `followup_1_subject`;
- `followup_1_body`;
- `followup_1_delay_days`;
- `followup_2_subject`;
- `followup_2_body`;
- `followup_2_delay_days`;
- `followup_3_subject`;
- `followup_3_body`;
- `followup_3_delay_days`.

Если follow-up колонки пустые, создается только первое письмо.

### 5.3. Правила импорта

- Строки без email не импортируются.
- Строки без темы или текста получают статус `blocked`.
- Дубли по email в рамках одного импорта подсвечиваются.
- Дубли по email в базе не создают нового лида, но могут создать новый черновик, если пользователь подтвердит.
- Если mailbox из Excel не найден, черновик создается с ошибкой.
- Если `send_after` в прошлом или пустой, письмо может быть отправлено при ближайшем запуске.
- Импорт должен сохранять `source_file`, `source_row_number`, `import_id`.

## 6. Сущности

### 6.1. Import

Поля:

- `id`;
- `file_name`;
- `file_type`;
- `status`;
- `rows_total`;
- `rows_ready`;
- `rows_blocked`;
- `rows_skipped`;
- `created_at`;
- `completed_at`;
- `error_report`.

### 6.2. Lead

Поля:

- `id`;
- `email`;
- `company`;
- `contact_name`;
- `position`;
- `website`;
- `domain`;
- `segment`;
- `city`;
- `notes`;
- `source`;
- `validation_status`;
- `validation_reason`;
- `status`;
- `created_at`;
- `updated_at`.

### 6.3. Draft

Персональный черновик письма.

Поля:

- `id`;
- `import_id`;
- `source_row_number`;
- `lead_id`;
- `campaign_id`;
- `conversation_id`;
- `sequence_step`;
- `mailbox_id`;
- `to_email`;
- `subject`;
- `body_text`;
- `body_html`;
- `send_after`;
- `status`;
- `error_reason`;
- `created_at`;
- `updated_at`.

Статусы:

- `draft`;
- `ready`;
- `blocked`;
- `queued`;
- `sent`;
- `cancelled`;
- `skipped`.

### 6.4. Sequence

Цепочка писем.

Поля:

- `id`;
- `campaign_id`;
- `name`;
- `status`;
- `created_at`;
- `updated_at`.

### 6.5. Sequence Step

Шаг цепочки.

Поля:

- `id`;
- `sequence_id`;
- `position`;
- `delay_days`;
- `subject`;
- `body_text`;
- `body_html`;
- `requires_approval`;
- `created_at`;
- `updated_at`.

### 6.6. Conversation

Диалог с конкретным email/лидом.

Поля:

- `id`;
- `lead_id`;
- `email`;
- `campaign_id`;
- `import_id`;
- `status`;
- `classification`;
- `last_message_at`;
- `next_action`;
- `ai_summary`;
- `created_at`;
- `updated_at`.

Статусы:

- `active_sequence`;
- `waiting_reply_review`;
- `manual_reply_needed`;
- `paused`;
- `completed`;
- `positive`;
- `negative`;
- `not_target`;
- `bounced`;
- `unsubscribed`.

### 6.7. Message

Письмо в диалоге.

Поля:

- `id`;
- `conversation_id`;
- `lead_id`;
- `campaign_id`;
- `mailbox_id`;
- `direction`;
- `type`;
- `status`;
- `subject`;
- `body_text`;
- `body_html`;
- `message_id_header`;
- `in_reply_to`;
- `references_header`;
- `tracking_id`;
- `reply_classification`;
- `reply_classification_source`;
- `sent_at`;
- `received_at`;
- `created_at`.

Типы:

- `outreach`;
- `followup`;
- `reply`;
- `auto_reply`;
- `bounce`;
- `test`;
- `warmup`.

### 6.8. Sending Queue

Очередь отправки.

Поля:

- `id`;
- `draft_id`;
- `conversation_id`;
- `lead_id`;
- `campaign_id`;
- `mailbox_id`;
- `sequence_step`;
- `status`;
- `requires_approval`;
- `approved_at`;
- `scheduled_at`;
- `attempts`;
- `last_error`;
- `created_at`;
- `updated_at`.

Статусы:

- `pending`;
- `retrying`;
- `running`;
- `sent`;
- `failed`;
- `cancelled`;
- `cancelled_reply_received`;
- `cancelled_by_user`.

## 7. Правила остановки цепочки

### 7.1. Любой живой ответ

Если пришел обычный ответ:

- conversation получает статус `waiting_reply_review`;
- enrollment/sequence для лида останавливается;
- будущие письма очереди отменяются;
- пользователь должен принять решение.

### 7.2. Positive reply

Если ответ классифицирован как `positive_reply`:

- conversation получает статус `positive`;
- будущие follow-up отменяются;
- диалог попадает в "Требуют решения";
- пользователь может ответить вручную или сгенерировать ответ.

### 7.3. Negative reply

Если ответ классифицирован как `negative_reply`:

- conversation получает статус `negative`;
- будущие follow-up отменяются;
- лид больше не должен получать автоматические письма в рамках этой кампании.

### 7.4. Not target

Если ответ классифицирован как `not_target`:

- conversation получает статус `not_target`;
- будущие follow-up отменяются;
- пользователь может пометить компанию/домен как неподходящий.

### 7.5. Unsubscribe

Если найден отказ от коммуникации:

- email или домен добавляется в suppression list;
- все будущие письма отменяются;
- conversation получает статус `unsubscribed`.

### 7.6. Auto-reply

Автоответ не должен автоматически считаться живым ответом.

Правила:

- если найдена дата возвращения, предложить перенести следующий follow-up;
- если дата не найдена, предложить продолжить по расписанию или сдвинуть follow-up;
- пользователь должен видеть автоответ отдельно;
- автоответ не должен попадать в positive reply rate.

## 8. Классификация ответов

На MVP допустим rule-based классификатор.

Классы:

- `positive_reply`;
- `neutral_reply`;
- `negative_reply`;
- `auto_reply`;
- `unsubscribe`;
- `not_target`;
- `bounce`;
- `unknown`.

Пользователь может вручную исправить классификацию.

Ручная классификация важнее автоматической.

## 9. AI-анализ

### 9.1. MVP

На первом этапе сервис должен готовить данные для внешнего AI-анализа через экспорт.

Минимальный экспорт `jsonl`:

```json
{
  "lead": {
    "email": "client@example.com",
    "company": "Компания",
    "contact": "Имя",
    "segment": "Рестораны"
  },
  "conversation": {
    "status": "positive",
    "classification": "positive_reply",
    "messages": [
      {
        "direction": "outbound",
        "type": "outreach",
        "subject": "Тема",
        "body": "Текст письма",
        "sent_at": "2026-07-15T10:00:00Z"
      },
      {
        "direction": "inbound",
        "type": "reply",
        "subject": "Re: Тема",
        "body": "Текст ответа",
        "received_at": "2026-07-15T13:00:00Z",
        "classification": "positive_reply"
      }
    ]
  }
}
```

### 9.2. Следующий этап

После MVP можно добавить встроенный AI-анализ:

- краткое резюме диалога;
- интерес/не интерес;
- извлечение боли;
- извлечение возражений;
- рекомендация следующего действия;
- генерация ответа;
- анализ удачных формулировок;
- сравнение сегментов.

## 10. Метрики

Главные метрики должны считаться по outreach, без прогрева и тестов.

Метрики:

- отправлено первых писем;
- отправлено follow-up;
- уникальные открытия;
- уникальные диалоги с ответом;
- positive replies;
- negative replies;
- auto replies;
- bounce;
- unsubscribe;
- reply rate;
- positive reply rate;
- среднее время до ответа;
- среднее количество касаний до ответа;
- диалоги требуют решения;
- отменено follow-up из-за ответа.

Ответы лучше считать по уникальным диалогам, а не по количеству входящих писем.

## 11. Безопасность и защита от ошибок

Обязательные правила:

- импорт не отправляет письма сразу;
- запуск требует preflight;
- письма без email/subject/body блокируются;
- отправка идет только через проверенные mailbox;
- SMTP/IMAP должны быть проверены;
- suppression list обязателен;
- при ответе цепочка останавливается;
- future follow-up отменяются;
- прогрев не смешивается с outreach;
- test письма не смешиваются с outreach-метриками;
- секреты не хранятся в git;
- после перезапуска очередь должна продолжить работу;
- зависшие `running` задачи должны возвращаться в `retrying`.

## 12. Приоритет реализации

### Этап 1. Excel и черновики

- импорт `.xlsx`;
- маппинг колонок;
- отчет по ошибкам;
- создание лидов;
- создание черновиков;
- экран черновиков.

### Этап 2. Очередь персональных писем

- постановка черновиков в очередь;
- отправка первых писем;
- отображение очереди;
- отмена и retry;
- preflight.

### Этап 3. Цепочки follow-up

- sequence;
- sequence steps;
- планирование follow-up;
- отмена follow-up;
- статусы цепочки.

### Этап 4. Диалоги

- conversation;
- связывание исходящих и входящих;
- карточка диалога;
- история переписки.

### Этап 5. Ответы требуют решения

- автостоп цепочки после ответа;
- экран "Требуют решения";
- ручные решения пользователя;
- автоответы отдельно.

### Этап 6. Экспорт и AI-анализ

- export `jsonl`;
- export `csv/xlsx`;
- фильтры экспорта;
- AI summary;
- AI next action.

## 13. Критерии приемки MVP

MVP считается готовым, если пользователь может:

1. Загрузить Excel с персональными письмами.
2. Увидеть ошибки импорта.
3. Исправить черновик письма.
4. Поставить готовые черновики в очередь.
5. Отправить первое письмо.
6. Запланировать follow-up.
7. Получить входящий ответ через IMAP.
8. Увидеть диалог с полной историей.
9. Убедиться, что future follow-up отменен после ответа.
10. Принять ручное решение по ответу.
11. Выгрузить диалоги в `jsonl`.
12. Посмотреть метрики без прогрева и тестовых писем.

